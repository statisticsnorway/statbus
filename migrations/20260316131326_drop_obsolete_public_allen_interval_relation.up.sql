BEGIN;

-- public.allen_interval_relation is obsolete — replaced by sql_saga.allen_interval_relation.
-- No columns, functions, or tests reference the public version.
DROP TYPE public.allen_interval_relation;

END;

- [x] **Fix worker concurrency issue**: Make the worker cleanup logic more robust to prevent race conditions from lingering connections after a deployment. This was causing `derive_statistical_unit` to fail intermittently. (2025-08-23)
- [x] **Fix post-import data derivation**: Corrected the import system to explicitly trigger `derive_statistical_unit` tasks after an import job completes. The previous mechanism relied on an incorrect `after_procedure` and unreliable statement-level triggers. (2025-08-25)
- [x] **Fix timeline fan-out issue**: Refactor the `public.timepoints` view to be correct and performant. The old view used deeply nested hierarchical joins, causing memory exhaustion and failing to capture all change boundaries. The new view uses a simplified, multi-stage approach that correctly gathers all timepoints (including `infinity`) without crashing. **REGRESSION**: The fix caused failures for `timeline_enterprise`. **FINAL FIX**: The view logic has been corrected to properly propagate timepoints from child establishments to their parent legal units, completing the hierarchical aggregation. (2025-08-30)
- [x] **(PROTOTYPED & BATTLE-TESTED) Battle-test and fix `timepoints` performance**: The `timepoints` view is unscalable and crashes on large datasets. Replaced with a physical table and a batched refresh procedure. The prototype has been validated on the large `statbus_no` dataset, uncovering and fixing a fan-out issue in the timeline aggregation logic. The architecture is now proven.
- [x] **Fix `statistical_unit_refresh` regression**: The recent change to `statistical_unit_refresh` to use `AND` instead of `OR` was causing test failures. The logic has been corrected to handle both full and partial refreshes correctly.
- [x] **Integrate prototype**: Integrate the battle-tested "materialize and batch" architecture from the `prototype` schema back into the main `public` schema via new migration files.
- [x] **Implement partial `statistical_unit` refresh**: Develop a robust mechanism for partial, targeted refreshes of the `timeline_*` and `statistical_unit` tables. This requires a dynamic, hierarchical lookup of all affected units. This has been improved to also find and refresh obsolete parent units from historical data, ensuring correctness when relationships change over time.
- [ ] **Add triggers for automatic refresh**: Implement database triggers on the core temporal tables (`legal_unit`, `establishment`, `activity`, etc.). These triggers will automatically queue a partial refresh task in the worker system whenever a relevant record is changed, ensuring the `statistical_unit` view is kept up-to-date.
- [ ] **Refactor `process_legal_unit`**: Remove the vestigial `tax_ident` column from the temporary table in `import.process_legal_unit` to improve clarity.

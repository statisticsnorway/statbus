# Upgrade-resume structural fix — ONE invariant, applied uniformly (final design for sign-off)

**Status: SHIPPED design record** (converged 2026-05-27, architect; all eight pieces landed — commit refs inline; restore-selection passages updated to the STATBUS-039 identity-keyed reality, STATBUS-043 2026-07-12). The King chose the principled root fix and then sharpened it to a single invariant. The diagnosis this answered is archived at `doc/archive/recovery-arc-flaw-timeoutstartsec.md`; the decision model this machinery serves is `doc/upgrade-recovery-model.md`.

## The unifying principle (the King's OOM insight)

**Nothing is unkillable** — OOM, reboot, SIGKILL can hit any process at any moment. So "never killed" (the earlier DETACH idea, "make boot-migrate unkillable") was the *wrong* goal. The right invariant, applied **uniformly to every step**:

> **A kill anywhere leaves a clean, resumable state; re-running converges (crash-safe + idempotent-resume); and advancing work is progress-protected (it survives the watchdog while it advances; a genuine hang is caught).**

The root defect was that the exit-42 **resume** ran heavy DB-size-scaled work in systemd's **start** phase under a **fixed** `TimeoutStartSec` — a budget that can never bound DB-size-scaled work, so on NO (32 GB) it was killed every cycle → 40 h loop. The fix is not to dodge the kill; it is to make every step satisfy the invariant so a kill is *survivable and convergent* everywhere.

## How each mechanism meets the invariant

| Step | crash-clean + resumable | progress-protected (liveness) |
|---|---|---|
| **completion** | FIX A: `completed`-UPDATE + flag-removal run *before* forensics (shipped `4d9b1e075`). | — |
| **tar** (archive) | ATOMIC: `.tmp`+fsync+rename; prune reaps `.tmp` orphans (shipped `33a9efc80`). | **CHANGE 1:** `tar --checkpoint=N --checkpoint-action=…` emits per-checkpoint progress → feeds the progress-gated watchdog; a write-hang stops checkpoints → watchdog fires. |
| **rsync** (backup) | **CHANGE 2:** persistent dir, committed by atomic dir-rename (`active`↔`syncing`); rsync idempotent → a killed rsync is *finished* by re-running. | rsync output advances the watchdog. |
| **migrate** (incl. boot-migrate) | each migration file commits in its own transaction; `db.migration` version table makes `migrate up` idempotent/resumable. | the per-migration `[migrate] ▶ applying …` line already fires `WATCHDOG=1` (`migrate.go:644-651`). |
| **the resume** | runs **active-phase** (B1) — the unit is honestly "running while it works". | progress-gated watchdog (#3) catches a true hang. |

## The pieces (single inline path — no DETACH, no `--link-dest`, no F1 carve-out)

1. **FIX A** — reorder completion before forensics. *(shipped `4d9b1e075`)*
2. **B1** — move `READY=1` **and** the two `LISTEN` calls to after cheap-init (`:1505`) but before `recoverFromFlag` (`:1517`) [Option Y]. Resume runs active-phase under `WatchdogSec`. *(shipped — code `702402cd0`, doc-flip `1b6b3043c`; boot-migrate-move folded in; advisory-lock keys verified distinct — `upgrade_daemon` vs `migrate_up` — so boot-migrate under the held lock is safe.)*
3. **Progress-gated watchdog** *(shipped `e90710230`)* — the active-phase ticker pings `WATCHDOG=1` **only if the pipeline advanced** within `stallThreshold`. `lastAdvanceAt` on `ProgressLog`, bumped by `ProgressLog.Write` (step boundaries) + a `PrefixWriter` `onLine` callback threaded through `runCommandToLog` (subprocess output — migrate/docker/rsync AND the tar's `--checkpoint` echoes). Hang → no advance → ticker stops pinging → `WatchdogSec` fires → unit `failed` (bounded). The heart of "advancing survives, hung is caught." **AS SHIPPED (`e90710230` + `4963e8149` + `615d9948a`):** the two prior unconditional tickers (reconnect-scoped + applyPostSwap-remainder) are COLLAPSED into ONE gated loop (`runGatedWatchdogTicker`, `watchdog.go`) started at the TOP of `applyPostSwap` (before config-generate, so the pre-reconnect silent subprocess steps — docker pull etc. — are covered too; a `progress.bump` on entry opens the gate from tick 1). It covers config-generate → docker pull → docker up → health → reconnect → migrate → step-11 up → archiveBackup. `stallThreshold = applyPostSwapStallThreshold = 3 m`, cadence 30 s ⇒ a genuine hang in an OUTPUT step trips at ≈ 5 m of silence (3 m gate-close + 120 s WatchdogSec). **Uniform principle (refined with team-lead):** GATE every output-emitting step (output distinguishes slow-from-hung); DEFER (always-ping) only the two genuinely-SILENT-but-BOUNDED blocking steps — **migrate/recreate** (bounded by `runCommandToLog` 30 m + #7) and **reconnect** (bounded by `connect()`'s new 5-min ctx deadline + #7). The reconnect bound is a genuine latent fix: `connect()` had NO connect_timeout and the caller ctx had no deadline, so a hung `pgx.Connect` blocked forever → deferring its gating would ping forever (the blind-unbounded hole). pgx honours a ctx deadline across the whole connect incl. the startup/auth handshake (`pgconn` `contextWatcher.Watch(ctx)`), verified by source. Go guards: `TestShouldPingWatchdog_*`, `TestRunGatedWatchdogTicker_*`, `TestConnectBoundedByConnectTimeout`.
   - **Coverage (engineer finding, #2 trace):** the existing ticker (`service.go:3665`) starts AFTER the pre-reconnect applyPostSwap steps — `config generate` (`:3532`), `docker compose pull` (`:3539`, 5-min timeout), `docker up` (`:3551`), `waitForDBHealth` (`:3565`) — each of which fires ONE `WATCHDOG=1` at its `progress.Write` start then runs the subprocess SILENT. A cold large-image pull >120 s on a production-scale box thus blows `WatchdogSec` mid-pull = 1 kill (a real pre-#3 exposure; NOT a #2 regression — those steps were killable via `TimeoutStartSec` pre-#2 too). #3 MUST wrap these pre-reconnect steps with the per-line `PrefixWriter` bump (docker pull emits layer-progress lines; config-generate emits lines) so a live-but-slow pull keeps the watchdog fed — not just the post-reconnect remainder. Update the unit-file comment to the full-coverage state when #3 lands. **DONE (`e90710230` + follow-up):** the single gated ticker is started at the TOP of `applyPostSwap` (before config-generate), so it covers the pre-reconnect steps too; `onAdvance = progress.bump` is threaded into config-generate, docker pull, docker up db, step-11 up, plus migrate/recreate, so a live-but-slow pull keeps the gate fed and a hung one (no output > 3 m) trips. `applyPostSwap` bumps `lastAdvanceAt` on entry so the gate is open from the first tick. Unit-file comment updated to full-coverage.
   - **Migrate-path liveness — RESOLUTION (b), signed off (corrected from (a) after verifying the bound):** the silent-statement concern is bounded by an EXISTING mechanism, not a new poll. `runCommandToLog` (`exec.go:124-141`) wraps every step in `CommandContext(timeout)` + `prepareCmd` (`Setpgid` + `Cancel` = `SIGKILL` to the whole **process group** + `WaitDelay`, `exec.go:35-47`). So a hung migrate step is killed — `psql` grandchild included → connection drops → the in-transaction migration rolls back — at the step's OWN timeout: **boot-migrate 5 min** (`service.go:1553`), **resume-migrate 30 min** (`service.go:3752`). Therefore #3 **defers gating during the migrate step**: keep `WATCHDOG=1` pinging through the `runCommandToLog("migrate")` call (so systemd doesn't kill at `WatchdogSec=120 s` mid-legit-long single-statement migration), bounded by that step's own timeout — an EXPLICIT documented defer, NOT a blind-forever ping (task-#37 was blind + UNbounded; this is blind-but-step-timeout-bounded). #7 (N=30 min cumulative-activating) converts a deterministically-hung-migrate LOOP to terminal `failed`. **Worst case hang→failed ≈ 30 min** — meets the King's invariant (detected + killed, not left looping). Hung migration is rare anyway (no non-transactional migrations; lock-deadlock pre-mitigated by R1 quiesce-before-DDL). **No poller, no PGAPPNAME tag, no `runPsqlFile` inner-timeout** — the OUTER `runCommandToLog` already bounds + process-group-kills it (the earlier "`runPsqlFile` runs forever" reading was WRONG). **(a)** server-side `pg_stat_progress` liveness (~3 min catch, +poller machinery) is a clean FOLLOW-UP if faster hung-migrate detection ever matters — not needed for the wedge-fix RC. Test pair: long-migration-survives-#3 (a >3 min advancing/single-statement migrate is NOT killed by #3 — gating defers during migrate) / hung-migrate-bounded (killed by its `runCommandToLog` timeout → fails → loop → #7). **NB (architect, docker mode):** the host-side kill does NOT cleanly roll back — migrate runs psql IN the db container via `docker compose exec -T` (migrate.go:106), and docker-exec doesn't forward SIGKILL, so the in-container psql is ORPHANED (txn open, locks held, can commit-after-rollback). #3's defer + #7 bound the LOOP; **task #14** makes the kill CLEAN — a server-side `pg_terminate_backend`-by-app_name on migrate-timeout + a PGAPPNAME tag on the psql subprocess. Pre-existing hole, closed this RC. **SHIPPED `b176b7bea`:** `injectPsqlAppName` tags the migration psql `statbus-migrate-sql-<pid>` in both modes (host PGAPPNAME / docker `-e PGAPPNAME=`, since the host env doesn't reach the in-container psql); `runCommandToLog` returns an `errors.Is`-matchable `ErrCommandTimeout`; at all three migrate sites (resume / recreate / boot-migrate) a timeout calls `terminateMigrateOrphan` on the live queryConn BEFORE rollback — `pg_terminate_backend` WHERE `application_name LIKE 'statbus-migrate-sql%'` (PREFIX-match, not pid: the terminate runs in the SERVICE process which can't know the migrate child's pid; the mutex serializes so one backend exists) excluding `pg_backend_pid()`. The install-time cleaners (`checkSessionsClean`/`cleanOrphanSessions`) were reconciled to also match the new prefix (else the retag would make them miss the orphan class they exist for). Guards `TestInjectPsqlAppName_*`, `TestMigrateOrphanTerminateSQL`, `TestErrCommandTimeout_IsMatchable`.
4. **Unit-reconcile** — `checkServiceDone` must **hash-compare** the on-disk unit to the rendered template (today it only runs `systemctl --user is-active`, so a healthy box never gets a drifted unit rewritten — `install.go:797-809`). Differ → false → `runInstallService` rewrites (`:1626`/`:2143`) → **then `daemon-reload` + restart so the new `WatchdogSec`/`TimeoutStartSec` actually arm** (a file write alone is inert). HARD DEP of #3. *(shipped `32c4174bc` — unit is copied verbatim ⇒ byte-compare on-disk vs repo template; re-arm restart gated on `!postUpgradeFixup`.)*
5. **CHANGE 1 — tar emits real progress** (replaces DETACH) *(shipped `1f37b4210`)*. `archiveBackup`'s tar gains `--checkpoint=1000 --checkpoint-action=echo=…` whose per-checkpoint lines flow through `runCommandToLog`'s `PrefixWriter` `onLine` → `progress.bump`, advancing `lastAdvanceAt`. The tar runs **inline** under the progress-gated watchdog like every other step. **One path** — identical under systemd (watchdog fed) and inline `./sb install` (no systemd, just runs). No `systemd-run`, no `NOTIFY_SOCKET` branch, no second code path. **CAPABILITY-GATE (resolution to the dev-box bsdtar gap the plan missed):** `--checkpoint` is GNU-only; `hostTarSupportsCheckpoint` probes `tar --version` once (cached, fail-safe) and appends the flags only on GNU tar — bsd/libarchive (macOS dev) runs a plain tar, harmless (no systemd to feed). Gated on tar *capability*, NOT GOOS/`NOTIFY_SOCKET`, so "one path" holds. **Also fixed a pre-existing latent gap:** archiveBackup's `runCommand` 5-min default would time out a 35 GB tar (→ no forensic archive); now `runCommandToLog` with a 60-min outer ceiling (the watchdog is the real hung-tar bound; post-FIX-A a timeout-kill is harmless). Guards: `TestTarSupportsCheckpoint`, `TestCheckpointLineBumpsLastAdvance`.
   - **Correctness (verified GNU-tar semantics):** `--checkpoint=N` fires every N records *processed* (not a wall-clock). A write-hang → records stop → checkpoints stop → `lastAdvanceAt` stops → ticker stops pinging → `WatchdogSec` fires. **Checkpoint-real, not a blind ticker** — does not recreate the task-#37 9h54m blind-watchdog hang.
6. **CHANGE 2 — persistent incremental backup dir, committed by ATOMIC DIRECTORY RENAME** *(shipped `1862e4e70`; restore-selection later superseded by STATBUS-039 `5eacd6305` — this passage describes the shipped identity-keyed state)* (replaces `--link-dest`; supersedes the earlier `.complete`-marker design — the King's refinement, 2026-05-27). **AS SHIPPED:** `backupDatabase` rename state machine extracted to `prepareBackupSnapshotDir` (DB/docker-free, unit-testable) + `syncTree` fsync-of-contents before the commit rename (heartbeat-covered) + commit `rename(syncing→active)`. `backup_path = …/pre-upgrade-active` (a mutable pointer; `backupStamp` kept for tar+logs only). **Restore selection is IDENTITY-KEYED (STATBUS-039):** `restoreDatabase` consumes ONLY the recorded `flag.BackupPath`/`row.backup_path` — the old `pickLatestBackup` recency scan was DELETED as a silent-loss path (a fallback that guesses "latest" can restore the wrong snapshot). Serialization = the persistent flag file (a competing actor routes through `RecoverFromFlag`→resume, never a fresh backup). `purgeOrphanBackups`+`pruneBackups` skip the managed dirs (`isManagedBackupDir`). Guards: `TestPrepareSnapshot_*`, the identity-contract restore tests (formerly `TestPickLatestBackup_*`), `TestIsManagedBackupDir`, `TestReconcileSkipsManagedDirs`. The rename state machine (active/syncing) is unchanged and current. The marker design dropped atomic-rename because it assumed atomic-rename needs a *fresh copy* (the shipped tar's `.tmp`→rename), which a persistent-incremental dir precludes. It doesn't: rename the **persistent dir itself** aside and back — incrementality preserved (content survives the rename) AND atomic clean/dirty restored (the dir **name** is the state). `backupDatabase` (`exec.go:261`) keeps the persistent incremental rsync target and commits via one atomic `rename(2)`:
   1. if `pre-upgrade-active/` exists → `rename(active → syncing)` (atomic; **content preserved** = the incremental base). If `active` is absent, a `syncing` left by a killed run is already the base — go straight to rsync; **never delete it** (rsync `--delete` reconciles a partial `syncing` to the source incrementally — that partial IS what you resume from). `active` and `syncing` never coexist from our sequence (architect-confirmed: after the aside-rename `active` is absent until the final rename consumes `syncing`), so there is NO cleanup branch and NO `rm`; an unexpected coexistence makes the aside-rename fail loudly = fail-fast, which is correct.
   2. `rsync --delete <db> → pre-upgrade-syncing/` (incremental, idempotent-resumable).
   3. fsync the synced dir before the commit (mirrors the shipped ATOMIC tar's fsync — durable across power-loss).
   4. `rename(syncing → active)` — `active` is guaranteed ABSENT here, so this is a clean atomic commit. The backup is complete **iff** it is named `pre-upgrade-active/`.
   - **A partial is structurally invisible (identity-keyed, STATBUS-039):** `restoreDatabase` reads ONLY the recorded `flag.BackupPath`/`row.backup_path`, and a dir named `syncing` is never recorded — so a partial backup cannot be restored by construction, with no marker-check to forget and no recency scan to guess wrong. `pruneBackups` → near-noop on the managed dirs; historical retention in the tars (`pruneArchives`, keep 3).
   - **Why rename dominates the marker:** (a) one atomic metadata op commits "clean" — the SAME pattern as the shipped ATOMIC tar, one mental model across both backup mechanisms; (b) the state IS the name — no second artifact (dir + marker) that can contradict; (c) **fail-safe by construction** — consumers look only for the clean name, so a partial can never be trusted even by forgetful consumer code (the marker required every reader to remember the check); (d) simpler durability ordering (fsync data → rename) than the marker's (fsync data → fsync marker → fsync parent).
   - **Idempotent resume (no deletion ever):** a kill mid-sync leaves `pre-upgrade-syncing/` (partial); re-run rsyncs `--delete` straight into it (reconciling the partial to the source) and commits via the final rename. The dir is never `rm`'d — that would discard the incremental base and force a full re-sync.
   - **Single-dir safety (unchanged argument):** `rename(active→syncing)` hides the last-good during the sync window, but `backupDatabase` runs at pre-swap, **before any destructive step** (migrate `:3726`/swap `:3424` later). A kill *during* backup ⇒ nothing destructive happened ⇒ live DB intact at pre-N ⇒ **no rollback artifact needed**; by the time a later step could fail, `pre-upgrade-active` is re-committed. The hidden-last-good window coincides exactly with the nothing-destructive-yet window. Safe.
   - **Durability (refined — architect):** step 3 must fsync the **file DATA** in `syncing/` (rsync `--fsync`, or sync the dir contents) BEFORE the step-4 rename — fsync on the *directory* persists entry names, not bytes. `active` must be named only after the bytes are durable, else the fail-safe degrades. (fsync the parent after the rename is optional: a lost rename just leaves `syncing` = redo = acceptable.)
   - **Integration holes identified at review and CLOSED in the CHANGE-2 commit (historical build notes — the H2 mutable-pointer invariant below is the one that remains load-bearing):**
     - **H1 (must-fix):** `service.go:3226` (`backupTmpPath`) independently builds the OLD `pre-upgrade-<stamp>.tmp` name in the `executeUpgrade` flow — a second construction site outside `backupDatabase`. Trace its dataflow and reconcile to `active`/`syncing`.
     - **H2 (must-confirm + test):** `flag.BackupPath` (`service.go:235/397`, consumed `:799-828` for forward-recovery auto-restore) becomes `…/pre-upgrade-active` — a now-**mutable** pointer. Safe ONLY because the upgrade mutex serializes upgrades (no second `backupDatabase` `--delete`-overwrites `active` before this upgrade's recovery consumes the pointer; the mutex is held until recovery completes). NEW invariant the rename introduces (per-stamp paths were immutable). State it explicitly + TEST it; if upgrades could overlap, recovery would restore the WRONG snapshot.
     - **H3 (must-address):** the per-`<stamp>` is load-bearing beyond the 4 fns — `pruneBackups` derives `upgrade-logs-<stamp>` (`exec.go:555-558`) to trim correlated logs, and a reconcile fn builds a map of all `pre-upgrade-*` dirs (`exec.go:~952-1015`). Re-derive the log-correlation stamp from the UPGRADE/tar identity (the per-stamp tar still carries it), and rework the reconcile-map for the single `active` dir + per-stamp tars.
7. **Liveness guard** *(shipped `5bf9e9c02`)* — trip the upgrade unit when it has been NOT-healthy-stable for cumulative N=30 min, regardless of restart count (NO looped 40 h under `StartLimitBurst=10/600` undetected — a slow loop never trips the burst cap, the Item-L blind spot). **AS SHIPPED:** an EXTERNAL sidecar — paired `ops/statbus-upgrade-liveness@.{timer,service}` (timer OnUnitActiveSec=5 m + Persistent + OnBootSec; oneshot service runs `sb upgrade liveness-check`), installed with the same verbatim-copy + byte-compare reconcile as the upgrade unit (new install step). The observer reads `systemctl show` ActiveState/SubState/ActiveEnterTimestamp, computes HEALTHY-STABLE (active+running + active >2 m + no `public.upgrade` row in_progress; DB-unreachable ⇒ not-stable but does NOT trip alone), and persists `last_healthy_at` in its OWN `tmp/upgrade-liveness-state` (survives the unit's restarts — the cumulative-not-healthy anchor `NRestarts`/`ActiveEnterTimestamp` can't give; heartbeat-staleness refuted because a slow loop rewrites a fresh heartbeat each cycle). TRIP = stop the unit + mark any in_progress row `failed` (clean thin DB call; `./sb install` reconciles otherwise) + **LOUD Slack callback** (`STATBUS_LIVENESS_TRIPPED`, required — unattended deploys are Slack-monitored) + dedup sentinel (stop+notify once; cleared on recovery or by `./sb install`). N=30 m matches #3's per-cycle worst case. Decision is a pure fn (`livenessDecision`); guards `TestLivenessDecision_*` (incl. the no-false-trip-on-legit-3-retry boundary), `TestParseUnitStatus*`, `TestLivenessStateRoundTrip`, `TestLivenessUnitsMatchRepo_*`. It is the universal backstop the #3/#12 per-cycle bounds (migrate 30 m, reconnect 5 m) rest on.
8. **boot-migrate folds in (no carve-out)** — move `boot-migrate-up` (`service.go:1491`) to **after `READY=1`** so it runs active-phase under #3. It already emits per-migration progress; migrations are transactional + version-table-resumable (verified below). A long-but-advancing boot migration survives, a hung one is caught, a kill leaves a clean resumable state. The earlier "F1 honest residual" **dissolves**. *(shipped with #2: `702402cd0`.)*

## Verification that the invariant holds for migrate (load-bearing — operator-confirmed)

- **No non-transactional migrations exist** (`rg -i "CONCURRENTLY|VACUUM|CREATE DATABASE|ALTER SYSTEM|REINDEX|CREATE TABLESPACE" migrations/` → only comments, zero statements). Each file runs via `runPsqlFile` with `ON_ERROR_STOP=on` (`migrate.go:245-305`); a kill mid-statement rolls back the open transaction; `migrate up` re-run resumes from `db.migration` (`migrate.go:617`/`365`). Crash-safe + idempotent-resume holds.
- **READY=1 can precede boot-migrate:** the main loop (post-READY) queries `db.migration` dynamically; nothing assumes "unit active ⇒ schema migrated." App serving is gated by the maintenance file (Caddy reads it) + app health — **independent of the upgrade unit's systemd READY**. NOTIFY processing stays async in the main loop (post-boot-migrate). So the app never serves on a stale schema.
- **The one residual is already handled:** the ~ms window where a migration's outer transaction committed but the `db.migration` INSERT hasn't (`migrate.go:685-704`, canonical Layer-2). SIGKILL there → re-run "relation already exists" → forward-recovery fails → **rsync-restore** completes recovery (already designed, two inject classes). **Not an open residual.**

→ **F1 folds 100% clean. No carve-out, no "honest limit." The invariant is uniform.**

## Implementation sequence + dependencies

1. **#4 unit-reconcile** first (hard dep of #3; independently correct): checkServiceDone hash-compare + reconcile → daemon-reload + restart.
2. **#2 B1 + #8 boot-migrate move** (compose — both about where `READY=1` sits relative to heavy startup work; re-point scenario-18 `StallHere` to the remaining cheap-init window).
3. **#3 progress-gated watchdog** (depends on #2 active-phase + #4 armed watchdog): `lastAdvanceAt` on `ProgressLog`; `ProgressLog.Write` + `PrefixWriter` `onLine` (threaded through `runCommandToLog`) bump it; ticker gates on `sinceLastAdvance < stallThreshold`.
4. **CHANGE 1** (tar `--checkpoint` → progress) — depends on #3's `lastAdvanceAt`.
5. **CHANGE 2** (persistent rsync + atomic dir-rename `active`↔`syncing`; restore identity-keyed on the recorded `backup_path`) — independent of #2/#3; same `exec.go` backup family.
6. **#7 liveness** — independent; can land last.
- FIX A + ATOMIC already shipped.
- `stallThreshold`/N: exceed the longest realistic inter-output gap; the one false-positive vector (a single giant silent migration statement) is bounded by the 30-min step timeout + mitigable by a heartbeat line. Tunable.

## Test plan (each asserts the invariant for one mechanism)

- **scenario 27 (extended):** exit-42 resume, archiveBackup exceeds budget → row `completed`, unit `active`, **0** kills.
- **progress-tar-survives-under-watchdog:** a long *advancing* tar (checkpoints firing) is NOT killed.
- **hung-tar-trips-watchdog:** tar blocked mid-write (no checkpoints) → watchdog fires → `failed` (bounded), not active+hung.
- **persistent-rsync-resumable-after-kill:** kill backup mid-rsync → dir left named `syncing` → re-run finishes + renames `syncing→active`; the partial was never restorable (only the recorded `backup_path` is consumed, and `syncing` is never recorded).
- **restore-ignores-syncing-dir:** a `pre-upgrade-syncing` (partial) dir is structurally invisible BY IDENTITY (STATBUS-039): `restoreDatabase` consumes only the recorded `flag.BackupPath`/`row.backup_path`, and a `syncing` partial is never recorded.
- **migrate-killed-mid-flight-resumes-and-converges:** SIGKILL mid-migrate → re-run resumes from `db.migration`. (+ existing canonical-Layer-2 commit-before-INSERT → rsync-restore.)
- **hung-migrate-trips-watchdog:** migrate with no per-migration output > stallThreshold → watchdog fires.
- **slow-docker-pull-survives / hung-docker-pull-trips:** a live-but-slow `docker compose pull` (layer-progress lines advancing) is NOT killed; a pull hung with no output > stallThreshold → watchdog fires. Asserts #3 covers the pre-reconnect window (`:3532-3565`), not just post-reconnect.
- **drifted-unit-reconciled-on-healthy-box:** drifted unit (infinity/90) on a healthy box → `./sb install` → checkServiceDone detects → rewritten to 120/120 → daemon-reload+restart arms timers.
- **liveness-trips-on-a-slow-loop:** repeated ~2.5 min activating cycle → `failed` within N min.
- **Go guards:** B1 ordering; progress-gated ticker (advance→ping, stall→no-ping); checkServiceDone hash-compare; marker write/refuse; tar `--checkpoint` callback bumps `lastAdvanceAt`. Existing FIX A + ATOMIC guards stay.

## Cross-context correctness (single path, so uniform)

- **Scheduled (active phase):** applyPostSwap already active; #3 protects all steps incl. checkpoint-tar; B1 READY-move is a no-op. ✓
- **Resume (exit-42 — target):** #2 moves it active; #3 protects; tar runs inline checkpoint-fed; #4 arms the watchdog. ✓
- **Inline `./sb install` (no systemd):** no `TimeoutStartSec`/`WatchdogSec` → nothing to kill; everything inline (tar `--checkpoint` callbacks are harmless heartbeats; #3 ticker is a no-op). **Same single code path — no fallback branch**, the whole point of CHANGE 1 vs DETACH. ✓

## Riskiest pieces + de-risk

1. **#3 false-positive on a legit silent step** — only vector is a single giant silent migration statement; bounded by the 30-min step timeout + heartbeat-line mitigation. Tune `stallThreshold`.
2. **#4 must re-arm, not just rewrite** — a rewritten unit does nothing until `daemon-reload`+restart; the scenario asserts the timers arm; #3 depends on it (call it out in code).
3. **CHANGE 1 `--checkpoint-action` wiring** — pick the form that flows through the existing `PrefixWriter` so the same `onLine` callback bumps `lastAdvanceAt` (e.g. `--checkpoint-action=echo=...` to the tee'd stream). Test "checkpoints advance lastAdvanceAt" directly.
- **No DETACH risk** — the unprecedented `systemd-run` mechanism is gone; CHANGE 1 reuses the existing inline + progress-watchdog machinery.

## Subsumption + residuals (honest)

- **Subsumes** task #8 (ATOMIC, shipped), task #7 (liveness = piece #7), **and task #6 (F1/boot-migrate)** — F1 folds with **zero carve-out** (verified: all migrations transactional; boot-migrate moves active-phase under #3).
- **No open residual.** The only migrate edge (commit-before-`db.migration`-INSERT) is already handled by rsync-restore. No "honest limit" remains — the invariant is uniform across tar, rsync, migrate, and the resume, in all three contexts.

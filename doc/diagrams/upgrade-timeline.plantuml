@startuml upgrade-timeline
' Authoritative sequence diagram for doc/upgrade-timeline.md.
' The `==` bands are the citable named phases — keep them in sync with the
' `##` section headers in upgrade-timeline.md so code comments can cite
' "see upgrade-timeline.md § <band>".
'
' TEST-NOTE CONVENTION (this diagram doubles as a visual test-coverage map):
' every failure-injection point carries a pale-yellow note
'     TEST <scenario-slug> (<NN>): <forward invariant the scenario must prove>
' framed as the GUARANTEE that must hold, NOT the failure. A point with no
' scenario is marked  NO TEST (gap): <unproven invariant>  so coverage holes
' are visible. Slugs are test/install-recovery/scenarios/ names. The
' install-recovery diagram uses the same convention.
skinparam backgroundColor #FEFEFE
skinparam sequence {
    ArrowColor #1565C0
    LifeLineBorderColor #90A4AE
    ParticipantBorderColor #1565C0
    ParticipantBackgroundColor #E3F2FD
    GroupBackgroundColor #ECEFF1
    GroupBorderColor #90A4AE
}

title StatBus Upgrade Timeline (single service, fail-fast one-shot, rollback to snapshot)

participant "Operator / CI" as cici
participant "systemd (user)" as systemd
participant "Upgrade Service\nstatbus-upgrade@$USER" as service
participant "PostgreSQL\npublic.upgrade + snapshot" as db

== Fresh install — seed restore (first boot, before any upgrade) ==
cici -> db : ./sb install (StateFresh) — Seed step:\n./sb db seed fetch (pull statbus-seed:<commit_short>) + pg_restore --single-transaction
cici -> db : ./sb migrate up — apply only the migrations newer than the seed
note over cici, db
  Fresh DB only: checkSeedRestored gates the Seed step OFF when the DB holds
  user data or applied migrations — a seed restore never overwrites a populated DB.
  Seed CREATION is a CI/dev activity — see DEVELOPMENT.md § Database seed.
end note

== Service boot ==
systemd -> service : start statbus-upgrade@$USER.service (Type=notify, Restart=always)
service -> db : advisory lock + LISTEN upgrade channel\n(cheap, bounded init)
service -> systemd : sd_notify READY=1\n(emitted here — heavy recovery below runs in the ACTIVE phase under WatchdogSec)
service -> db : boot migrate up — bring schema to HEAD (schema-skew guard)
service -> service : recoverFromFlag — reconcile a flag left on disk (flag-DRIVEN)
service -> db : completeInProgressUpgrade — reconcile an in_progress row with NO flag\n(flag-INDEPENDENT belt; the SOLE recovery for a pre-flight kill)
note over service, db
  Two-layer boot recovery. recoverFromFlag handles a kill AFTER the flag exists (Phase
  ladder: PreSwap / PostSwap / Resuming). completeInProgressUpgrade handles a kill BEFORE
  the flag exists — the claim -> writeUpgradeFlag window (see § Execute upgrade (pre-swap)).
  It rolls such an orphan to a named terminal state via verifyUpgradeGroundTruth.
end note
note over systemd, db #FFF9C4
  TEST COVERAGE — boot
  TEST startup-exceeds-timeoutstartsec (18): startup past TimeoutStartSec stays bounded —
    heavy work runs post-READY=1 under WatchdogSec, never the static start budget.
  TEST advisory-lock-before-db-ready (25): an advisory-lock attempt before the DB is ready
    exits 42 and systemd restarts cleanly — no start-loop wedge.
  NO TEST (gap): the belt rolling a flagless in_progress row to terminal is unproven —
    see the preflight-kill gap under § Execute upgrade (pre-swap).
end note

== Schedule + dispatch ==
cici -> db : ./sb upgrade apply-latest →\nUPDATE state='scheduled' + NOTIFY
db -> service : NOTIFY wakes the listener
service -> db : claim row WHERE state='scheduled' → in_progress\n(started_at set, error=NULL — NO flag on disk yet)

== Execute upgrade (pre-swap) ==
group pre-flight — NO flag on disk yet (row is in_progress, error=NULL)
    service -> db : pre-flight checks — downgrade guard, release assets, disk, signature
end
note over service, db
  Flag-absent window — spans the claim above through pre-flight, until writeUpgradeFlag.
  A kill HERE leaves the row in_progress | error=NULL with NO flag: recoverFromFlag finds
  nothing to reconcile, so completeInProgressUpgrade (§ Service boot) is the ONLY recovery
  — it rolls the row back with a named reason at the next boot.
end note
note over service, db #FFF9C4
  NO TEST (gap): preflight-kill — a kill in the claim -> writeUpgradeFlag window must converge
    the row to a named terminal state via the belt at next boot. Candidate scenario:
    killed-by-system-during-preflight-before-flag.
end note
group mutex held — Holder="service", flock LOCK_EX on the marker fd
    service -> service : writeUpgradeFlag — acquireFlock opens the marker (Phase=PreSwap)
    service -> db : maintenance mode on
    service -> db : take DB snapshot -> backup_path
    service -> service : git checkout target commit
    service -> service : replaceBinaryOnDisk -> stamp Phase=PostSwap
end
note over service, db
  Fail-fast: any step (pre- or post-swap) that errors OR hangs aborts the
  single attempt and routes to rollback (§ Complete / rollback). No retry.
end note
note over service, db #FFF9C4
  TEST COVERAGE — pre-swap (mutex held)
  TEST preswap-backup-kill (21): a kill during the DB snapshot converges the row to a named
    terminal — rolled_back, or failed if the restore also fails — never stranded in_progress.
  TEST preswap-checkout-kill (22): a kill during git checkout converges to terminal; the
    unswapped binary keeps serving the old version.
  TEST binary-swap-kill (16): a kill at the replaceBinaryOnDisk pivot is recovered (PostSwap
    resume or rollback) — never a half-swapped row that lies about the running version.
end note

== Binary-swap restart + resume (post-swap, on the NEW binary) ==
service -> systemd : exit 42 — self-update, request the ONE planned respawn
systemd -> service : restart unit on the new binary
note over service, db
  The new binary BOOTS FRESH — nothing continues in memory. recoverFromFlag (service.go:682)
  READS the flag's persisted Phase to learn where the prior process died. Phase progresses
  PreSwap "" (:197) -> PostSwap "post_swap" (stamped :403, before exit 42) -> Resuming (stamped
  :4339, as resume commits). The SAME dispatch runs on EVERY boot — the planned exit-42 here,
  or any unplanned-death restart.
end note
alt flag.Phase = PostSwap — the PLANNED exit-42 handoff (the ONE legitimate continue)
    service -> service : resumePostSwap (:774) -> stamp Phase=Resuming (:4339)
    group applyPostSwap — remaining steps run on the new binary
        service -> service : config generate + docker compose pull
        service -> db : start DB + wait health + reconnect
        service -> db : ./sb migrate up
        service -> service : start app services + health check
        service -> db : maintenance off + archive backup
    end
else flag.Phase = PreSwap — died BEFORE the binary swap
    service -> db : recoveryRollback (:822) -> roll back to snapshot (§ Complete / rollback)
else flag.Phase = Resuming — died DURING the resume (the one-shot LATCH)
    service -> db : recoveryRollback (:755) -> roll back, NEVER re-resume (§ Complete / rollback)
end
note over systemd, service
  recoverFromFlag is a 3-WAY DISPATCH on the persisted flag.Phase — "detect where you died ->
  jump to the right action", not a linear continue. PostSwap is the one legitimate continue;
  PreSwap and Resuming both roll back. Resuming is the latch: any non-planned restart while
  in_progress => died => rollback, never an infinite re-resume loop.
end note
note over service, db #FFF9C4
  TEST COVERAGE — phase dispatch + post-swap resume
  TEST resume-died-rolled-back-no-retry (29): a death during Phase=Resuming rolls back to the
    snapshot (UPGRADE_DIED_DURING_RESUME) and NEVER re-resumes — the one-shot latch (the
    Resuming branch above).
  TEST mid-migration-kill (17) / between-migrations (23) / migrate-killed-after-commit-before-recorded (08):
    a kill anywhere in migrate converges to a coherent terminal; the canonical commit<->record
    window RESTORES, never re-applies ("relation already exists" is the deterministic break).
  TEST migration-exceeds-unit-timeout (12) / migration-vs-worker-lock-bounded (13): a slow or
    worker-locked migration stays BOUNDED (heartbeat / pre-DDL quiesce) — never an infinite wedge.
  TEST killed-during-container-restart (15) / watchdog-fires-during-reconnect (19): a kill or
    stall in the restart + reconnect tail is recovered; the watchdog fires on a hung reconnect.
  TEST archivebackup-watchdog (26) / archivebackup-resume-active-phase (27): the active-phase
    archive-backup tar is watchdog-covered — a long-but-advancing backup is not falsely reaped.
end note

== Complete / rollback ==
alt all steps succeeded
    service -> db : mark completed_at (state=completed)
    service -> service : removeUpgradeFlag (mutex released)
    service -> db : supersede older rows + notify UI
    service -> cici : Slack callback — OK
else any step failed or hung (one attempt, NO retry)
    service -> service : rollback() — restore git + restore DB snapshot
    service -> db : restart services (docker compose up)
    service -> db : wait for DB health (30s)\nSYMMETRIC with applyPostSwap (service.go:3780) — must NOT reconnect before the DB is ready (the scenario-21 race)
    service -> db : reconnect (re-acquire authority lock) + write terminal state (bounded retry)
    alt snapshot restored cleanly AND services healthy
        service -> db : state=rolled_back (+ error) — healthy at old version
    else restore also failed
        service -> db : state=failed (+ error) — needs hands-on
    end
    service -> service : removeUpgradeFlag — ONLY on a landed write\n(else KEEP: dead-PID breadcrumb -> StateCrashedUpgrade / belt reconciles)
    service -> cici : Slack callback — FAILED siren (fires regardless of the write)
end
note over service, db #FFF9C4
  TEST COVERAGE — complete / rollback
  TEST killed-during-rollback (24): a kill mid-rollback() still converges to a terminal state
    on the next recovery pass.
  GAP -> FIX (scenario 21 RED; guard = preswap-backup-kill (21)):
  ROOT: rollback() restarted the DB then reconnected with NO DB-health-wait — asymmetric with
    applyPostSwap's waitForDBHealth(30s) (service.go:3780) — so the reconnect raced the DB
    restart and transiently failed. The terminal UPDATE was then single-shot, silently swallowed,
    and the flag removed unconditionally -> row stranded in_progress. The retry alone would only
    have papered over the missing wait.
  FIX (1) add waitForDBHealth(30s) after restart, before reconnect — the root fix, symmetric with
    the normal path (drawn above). (2) the terminal write retries the FULL reconnect (re-acquiring
    the upgrade_daemon AUTHORITY lock, service.go:2660 / 1922) + markTerminal on exhaustion +
    removeUpgradeFlag ONLY on a landed write (KEEP on failure). On lock-contention, YIELD to the
    authority — the belt reconciles; never bypass the lock.
end note

@enduml

name: pg_regress
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Images"]
    types: [completed]
  pull_request:
    branches-ignore:
      - 'ops/**'

jobs:
  pg_regress:
    # Skip if Images failed (only applies to workflow_run trigger)
    if: >-
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    concurrency:
      group: statbus_test
      cancel-in-progress: false
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: Determine ref and sha
      id: ref
      run: |
        if [ "${{ github.event_name }}" = "workflow_run" ]; then
          echo "ref=${{ github.event.workflow_run.head_branch }}" >> "$GITHUB_OUTPUT"
          echo "sha=${{ github.event.workflow_run.head_sha }}" >> "$GITHUB_OUTPUT"
        else
          echo "ref=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          echo "sha=${{ github.sha }}" >> "$GITHUB_OUTPUT"
        fi
    - name: Run tests on remote server
      uses: appleboy/ssh-action@v1.2.0
      with:
        host: niue.statbus.org
        username: statbus_test
        key: ${{ secrets.SSH_KEY }}
        command_timeout: 60m
        # STATBUS-150 self-heal rides inside continous-integration-test (dev.sh) —
        # the SSH line below must match /etc/sshdoers:44's allowlisted template
        # on the test host EXACTLY, or the CI key's command is silently rejected.
        script: cd statbus && git fetch origin && git checkout -- . && git checkout ${{ steps.ref.outputs.sha }} && ./dev.sh continous-integration-test

# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "ma.statbus.org<-ops/cloud/deploy/ma"
on:
  workflow_dispatch:
  push:
    branches:
      - "ops/cloud/deploy/ma"

concurrency:
  group: ${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Deploy via upgrade service
        run: |
          install -m 600 /dev/stdin /tmp/key <<< "${{ secrets.SSH_KEY }}"
          ssh -i /tmp/key -o StrictHostKeyChecking=no \
            statbus_ma@niue.statbus.org \
            "cd ~/statbus && ./sb upgrade apply-latest"
          rm -f /tmp/key

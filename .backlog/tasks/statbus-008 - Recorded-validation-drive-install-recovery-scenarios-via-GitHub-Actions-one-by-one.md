---
id: STATBUS-008
title: >-
  Recorded validation: drive install-recovery scenarios via GitHub Actions (one
  by one)
status: In Progress
assignee:
  - operator
created_date: '2026-06-07 15:41'
updated_date: '2026-06-08 01:54'
labels:
  - install-recovery
  - validation
  - ci
dependencies: []
priority: high
ordinal: 8000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Drive each install-recovery scenario via GitHub Actions (`gh workflow run install-recovery-harness.yaml --ref master -f scenarios="<slug>"`), one at a time, so every pass/fail is RECORDED + queryable (vs ephemeral local runs). Confirms recovery behavior in reality and exercises the gate. Subsequent verdicts appended in notes.

Prereq per run: the Images workflow must have pushed the seed image (statbus-seed:<sha>) for the SHA first, else the harness build fails "manifest unknown" (ordering finding from the first attempt).

TALLY (--ref master @ 4e07dc4d):
- 0-happy-install: PASS — run 27096800159 (https://github.com/statisticsnorway/statbus/actions/runs/27096800159), 9m29s. First attempt 27096707578 failed on seed-image-not-yet-pushed timing; re-run green.
- 3-postswap-watchdog-reconnect: dispatched (in progress).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Baseline 0-happy-install driven + recorded (PASS)
- [ ] #2 The three sharpened-claim scenarios driven + recorded: watchdog-reconnect, migrate-killed-after-commit, archivebackup-resume
- [ ] #3 Each driven scenario's GitHub run URL + verdict captured in the notes tally
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
watchdog-reconnect (run 27097092218): FAILED — HARNESS BUG, not a real recovery failure. The scenario's systemd drop-in install used a here-document that collapsed newlines (log: `EOF[Service]Environment=...Environment=...EOF` on one line), so systemd couldn't parse the override and the unit stayed inactive — the scenario can't even stage its test (product code never exercised). Mechanic dispatched to fix the heredoc + assess scope (shared drop-in helper?). DRIVE-THROUGH PAUSED until the harness fix is committed + pushed; subsequent runs will be on the fixed SHA.

Harness fix pushed: 4e07dc4d5..2bc671ecf. Root cause was VM_EXEC's printf '%q' ANSI-C quoting collapsing the heredoc-over-bash-c; fixed to mktemp + local heredoc + scp + remote bash (the pattern the archivebackup scenarios already use). Fixed watchdog-reconnect AND the same LATENT bug in 1-boot-startup-timeout. Scope: 2 scenarios, NOT a shared helper. Pending: Images must build the seed image for 2bc671ecf, then re-run watchdog-reconnect, then continue (migrate-killed-after-commit, archivebackup-resume) on this SHA.

watchdog-reconnect re-run (run 27097723557 @ 2bc671ecf): harness heredoc fix WORKED ✓ (C15 drop-in installed cleanly, 'unit active with C15 env vars'). But FAILED on a NEW, deeper issue: the supervised upgrade unit did NOT transition the scheduled row to in_progress within 180s — row stayed 'scheduled', upgrade never started, so the C15 reconnect injection was never reached. Service was up + healthy (discovered 176 tags, verified images) but didn't pick up the scheduled upgrade. Either a REAL upgrade-service polling/NOTIFY bug or a scenario/supervised-path issue — engineer dispatched to diagnose (read-only). NOT a regression from our changes (none touched polling). Continuing drive-through with migrate-killed-after-commit (inline ./sb install path, independent of the supervised path) in parallel.

TALLY update: 0-happy-install PASS; watchdog-reconnect = harness-fix-confirmed but blocked on upgrade-pickup (under diagnosis); migrate-killed-after-commit dispatched.

watchdog NOTIFY fix (engineer, commit 3bb6d703d — verified correct/scoped: defines SHORT_SHA from HEAD_LOCAL, sends ./sb upgrade apply NOTIFY mirroring archivebackup-watchdog, fixes the stale 180s diagnostic) pushed 2bc671ecf..3bb6d703d. Operator driving: Images-green-for-3bb6d703d → re-run watchdog-reconnect (~12-15 min). The migrate INSTALL_VERSION fix (4568554b7) is now pushed too; migrate-killed-after-commit runs AFTER watchdog is green (one scenario at a time).

watchdog-reconnect re-run (run 27104216670 @ 3bb6d703d): NOTIFY FIX WORKED ✓ — NOTIFY → executeScheduled in 58s, stall held 180s > WatchdogSec=120s, NRestarts within tolerance (Race D fix holds), upgrade completed, flag absent, demo data intact. The CORE watchdog/reconnect recovery behavior is VALIDATED. Remaining failure: 'orphan backup(s) found' at test/install-recovery/lib/assertions.sh:100 — leftover backup files the assertion expects cleaned up. Engineer diagnosing: real product cleanup gap (pruneArchives / archiveBackup) vs over-strict assertion. Progress: harness heredoc + NOTIFY both fixed; one cleanup assertion left on this scenario.

ORPHAN-BACKUP VERDICT (engineer, run 27104216670): HARNESS/ASSERTION BUG — ZERO real orphans; the product cleaned up correctly. Root cause lib/assertions.sh:95: `grep -c .` exits 1 on zero matches; under the scenario's `set -euo pipefail` the count pipeline exits non-zero → the trailing `|| echo "0"` appends a 2nd 0 → count="0\n0" → fails the `= "0"` check. Reproduced locally. Introduced in commit 97fe00480 (the #12 active/syncing backup-scheme reconcile). SYSTEMATIC — latent in 7 scenarios that call this shared assertion (2-preswap-binary-swap-kill, 3-postswap-watchdog-reconnect, mid-migration-kill, migrate-killed-after-commit, 0-happy-upgrade, 5-install-seed-on-populated, container-restart-kill); watchdog is just the first to reach it post-NOTIFY-fix. Fix = ONE shared assertion (clears all 7): `|| true` after grep -c . + `tr -d ' \n'`. Classified TEST/harness (not recovery code) → applied autonomously (no King gate). watchdog-reconnect's actual recovery is FULLY VALIDATED (zero real failures); expect GREEN after the assertion fix + re-run.

✅ 3-postswap-watchdog-reconnect: PASS — run 27105191049 @ 8366440d9, 13m6s. FIRST failure-scenario green. Recovery validated end-to-end: NOTIFY → executeScheduled → upgrade → watchdog timeout correct (stall held > WatchdogSec, NRestarts ok) → reconnect clean → upgrade completes → orphan-backup cleanup verified. All 3 harness fixes held (heredoc 2bc671ecf, NOTIFY 3bb6d703d, assertion 8366440d9). The assertion fix also unblocks 6 sibling scenarios. RUNNING TALLY: 0-happy-install PASS; watchdog-reconnect PASS. NEXT: 3-postswap-migrate-killed-after-commit (inline; has INSTALL_VERSION fix 4568554b7 + the assertion fix; tests the rune-wedge: kill in the ~ms commit↔record window → forward fails 'relation already exists' → RESTORE → rolled_back).

3-postswap-migrate-killed-after-commit (run 27105528694 @ 8366440d9): FAILED — HARNESS bug (not product). The scenario passes the FULL 40-char SHA to `./sb upgrade schedule`; the version validator rejects it (vm-bootstrap.sh:360: 'invalid version ... expected vYYYY.MM.PATCH or sha-HEXHEX'). Needs the sha-<short-hex> form. Error is in a shared helper → likely affects other schedule-using scenarios. Mechanic dispatched to diagnose scope (shared vm-bootstrap helper vs scenario) + fix the version format + reconcile why `./sb upgrade apply 8366440d` worked but schedule<full> didn't. Harness fix (commit local). RUNNING TALLY: 0-happy-install PASS, watchdog-reconnect PASS, migrate-killed-after-commit = version-format harness bug (fixing).

migrate-killed-after-commit version-format VERDICT (mechanic, commit f018a75d8): CORRECT fix, verified. The minimal 'sha-<short>' fix wouldn't work — `./sb upgrade schedule` only accepts CalVer tags AND only UPDATEs existing 'available' rows, so it can't schedule untagged HEAD at all. Fix: use fabricate_scheduled_upgrade_row (direct INSERT), the SAME pattern the 3 sibling supervised scenarios already use for untagged HEAD; coverage preserved (still drives ./sb install -> executeUpgrade -> migrate-kill recovery). Isolated to this scenario (no other uses ./sb upgrade schedule). Mechanic also flagged a real PRODUCT issue -> filed STATBUS-010 (stale 'sha-HEXHEX' in upgrade.go:135 validator error message, retired rc.63). NEXT: push f018a75d8 -> Images -> re-run migrate-killed-after-commit. Harness-bug tally now 9, product bugs 0 (the stale message is a doc/message nit, not a recovery failure).

migrate-killed-after-commit re-run (run 27106097096 @ 14c3db9b8): FAILED — stall injection never fired (300s timeout). Stage-1 fabricate created the row (id=5, sha=14c3db9b8, state=scheduled), but the second `./sb install` (STATBUS_INJECT_AT=migrate-subprocess-killed-after-commit-before-recorded) didn't reach the migrate inject point. Open question: does `./sb install`'s inline StateScheduledUpgrade probe detect a fabricate_scheduled_upgrade_row row (that helper was built for the supervised NOTIFY path)? Engineer diagnosing (harness vs product; + scope: does fabricate+install break other inline scenarios?). Migrate is multi-layered: version-format fixed (f018a75d8), now stall-never-fires. If this becomes a deep harness rework, I'll document + move to an easier scenario (archivebackup-resume) for morning progress, then return.

migrate stall-never-fires ROOT CAUSE (engineer): HARNESS timing/staging, not product. Untagged HEAD makes the inline `./sb install` upgrade run buildBinaryOnDisk (make -C cli build, ~3-5 min) BEFORE applyPostSwap's migrate-up (skipped only if the binary is pre-staged at the target commit); migrate-killed-after-commit does NOT pre-stage, and STALL_MAX_WAIT_S=300 is too short for build+backup+pull+DB-start+migrate. The working sibling mid-migration-kill pre-stages (upload_sb_to_vm) AND budgets 900s; migrate got the worst combo. NOTE: f018a75d8 (fabricate) is the FIRST run to actually dispatch the inline upgrade (the old ./sb upgrade schedule errored before dispatch), which exposed this. Fix = Option A (chosen, harness-only): add upload_sb_to_vm after initial install (skip the build) + bump STALL_MAX_WAIT_S 300→900, matching the sibling. Engineer applying on top of f018a75d8. Tally: 10 harness/CI issues, 0 product.

migrate Option A re-run (run 27106941336 @ 75ceab1ff): FAILED — stall timed out AGAIN at 900s. So it's NOT (just) build-timing; the stall genuinely doesn't fire. CRITICAL: the stage-1 `./sb install` produced NO captured output (detached tmux → /tmp/stage1.log, not in artifacts) — we're BLIND on whether the second install dispatched the upgrade. The engineer's residual-risk hypothesis (inline trigger not dispatching) is now live. ACTION: engineer building an observability fix (surface /tmp/stage*.log to CI, shared helper → STATBUS-011) so we can SEE the detected state on the next migrate re-run; meanwhile operator drives 3-postswap-archivebackup-resume for parallel progress (one run at a time via the operator). Migrate parked pending the stage-log capture. If after that it's a deep PRODUCT dispatch issue, I'll document for the King + move on. Tally: 11 harness/CI issues, 0 product.

3-postswap-archivebackup-resume (run 27107825797 @ 75ceab1ff): FAILED — NRestarts delta=1 (the exit-42 RESUME was killed ONCE during the stalled archiveBackup tar; assertion expects 0). Recovery still completed + demo data verified, but the watchdog protection (applyExtendCtx ticker / READY=1-before-recoverFromFlag, plan piece #2) may not cover the RESUME-path tar. POTENTIAL FIRST PRODUCT FINDING (the 40h NO/rune wedge this scenario exists to catch). Architect diagnosing with adversarial rigor (real product gap vs test-expectation/timing; READ-ONLY; flag for the King — recovery code, no autonomous change). In parallel: operator re-running migrate-killed-after-commit on b80d725a7 (observability fix landed) to capture the stage-1 'Detected install state' + test the no-dispatch hypothesis (migrate timed out twice; pre-stage should make the stall fire in ~1-2 min, so 900s timeout ≈ inline trigger not dispatching). TALLY: 0-happy-install PASS, watchdog-reconnect PASS; archivebackup-resume = potential PRODUCT (architect verdict pending); migrate = diagnosing.

archivebackup-resume VERDICT (architect, run 27107825797): NOT the wedge — recovery is CORRECT (substate=running ⇒ active-phase; row=completed WHILE tar stalled ⇒ FIX A + plan-piece-#2/READY=1-before-recoverFromFlag held; full convergence passed: row completed, flag absent, data intact, archive atomic). The failure is the over-strict 0-kills assertion (scenario sh:485) firing on ONE harmless post-completion ACTIVE-phase kill (cause uncaptured this run; likely CX23 WatchdogSec-jitter/OOM under load; a ticker micro-gap is less likely but not excluded). No-flaky-tests path (architect applying now, local commits): rec1 = scenario diagnostics (journalctl+systemctl-status capture on the NRestarts-fail path + sample NRestarts/substate every 5s, fix the %30 t+30 skip); rec3 = doc-truth fix (doc/recovery/recovery-arc-flaw-timeoutstartsec.md:82-86 wrongly says 'FIX A alone, B1 dropped, resume keeps start-phase' — the FULL structural plan shipped; evidence service.go:1621 'plan piece #2, B1' + substate=running). Then push → re-run to PIN the kill → rec2 (relax to bounded check if benign / product-fix if micro-gap). En route surfaced a REAL latent PRODUCT gap → STATBUS-012 (boot-migrate-up service.go:1644 emits no WATCHDOG=1; large-DB boot-migrate >120s would be watchdog-killed). NET so far: 0 product RECOVERY bugs (wedge confirmed fixed), 1 latent product gap (filed), 1 doc-truth fix; the archivebackup-resume assertion is harness (relax pending the pinning re-run). Operator separately on migrate stage-log re-run.

migrate re-run (run 27108164190 @ b80d725a7 images): FAILED — but the OBSERVABILITY FIX (b80d725a7) WORKED and revealed the root cause (stage-1 log now in CI). The stage-1 `./sb install` died BEFORE state detection: '/sb is stale: built from b80d725a, HEAD is now 50fd4325 with cli/ changes' → self-heal `make -C cli build` → `go: not found` → exit 2. ROOT CAUSE = SHA-DRIFT (harness, not product/scenario-logic): the VM's pre-built ./sb (Images SHA b80d725a7) is stale vs the VM's git checkout (newer master 50fd4325 — master moved after Images / I pushed during the run), tripping ./sb's stale-binary self-heal which fails (no Go on the VM). So the migrate dispatch question is STILL untested (install died before it). Engineer fixing SYSTEMATICALLY: pin the VM git checkout to the run's SHA (not latest master) → ./sb coherent → no self-heal → robust to pushes-during-runs. GRIND PAUSED until the coherence fix lands (avoid burning more drifted VMs). Lesson: don't push while a run is in flight (origin only moves on explicit push; backlog autocommits stay local). Tally: 12 harness/CI issues, 0 product recovery bugs, 1 latent product gap (STATBUS-012).

migrate SHA-coherence — engineer CORRECTED my framing (commit c9f89f930). NOT master-drift, NOT a vm-bootstrap gap: vm-bootstrap already pins the run SHA (git checkout $local_commit, vm-bootstrap.sh:498). Real cause: Option A (75ceab1ff) added upload_sb_to_vm (run-SHA ./sb) but the scenario git-fetched HEAD WITHOUT checking it out — worktree stayed at the v2026.05.2 install-clone (commit 50fd4325), so run-SHA-binary vs v2026.05.2-worktree tripped freshness/check.go → self-heal `make build` → go-not-found → exit 2 before state detection. Sibling pre-staging scenarios (mid-migration-kill:112, watchdog-reconnect:148) already checkout HEAD; Option A omitted it. Fix c9f89f930: `git checkout $HEAD_SHA` after fetch, before upload → binary==worktree==run-SHA, coherent. CONSEQUENCES: (1) NO Go toolchain needed on the VM (buildBinaryOnDisk also skips, service.go:5073); (2) runs PIN their SHA → pushing mid-run is SAFE (my earlier 'don't push during runs' worry was unfounded). Scope: migrate ONLY. Pushed; re-run migrate after archivebackup-resume to finally test inline-dispatch (with observability). Tally correction: this is part of the migrate fix-chain (version-format → build-timing → missing-checkout), not a separate systematic issue.

archivebackup-resume kill CLASSIFIED (run 27109134019, via rec-1 diagnostics): status=75/TEMPFAIL — the process SELF-EXITED code 75 (NOT watchdog, NOT OOM [28M peak], NOT a signal). Journal: 00:25:32 'UPGRADE_DIED_DURING_RESUME ... rolled back to the snapshot. NO retry' → 00:25:55 exit status=75/TEMPFAIL → restart (NRestarts=1). PUZZLE (how-can-both-be-true): earlier run-1 + architect said row=completed + demo-data VERIFIED, but this shows 'rolled back to snapshot'. Architect resolving adversarially: (1) where exit-75 originates — the INJECTION (die vs stall) or a PRODUCT exit (internal archiveBackup timeout defeating the ticker); (2) final state completed-vs-rolledback; (3) rec-2 — relax the NRestarts=0 assertion (if benign/injection-die) or flag a product issue for the King. Operator separately: migrate-killed-after-commit re-run on c9f89f930 (coherence checkout fix) — first COHERENT run (past the freshness wall), will finally show the stage-1 detected-state + dispatch/stall. Tally: 2 green, ~12 harness/CI, 0 product recovery bugs confirmed, 1 latent product gap (STATBUS-012).

migrate coherent run (27109532127 @ c9f89f930): NO-DISPATCH HYPOTHESIS REFUTED — with stage logs now visible, the inline ./sb install DOES detect 'scheduled-upgrade (id=11)' + 'Dispatching inline upgrade'. But then 'crashed-upgrade' and the stall never fired (900s timeout, sh:205). KEY CLUE: current=2026.05.6-rc.03-149-gc9f89f930 == target=c9f89f930 → NO-DELTA suspected: the coherence checkout (git checkout c9f89f930) made the worktree git-describe = current = c9f89f930, so upgrade-to-c9f89f930 has no migration delta → migrate runs nothing → the migrate-subprocess inject never fires → stall never activates. (Tension: binary-coherence needs worktree=run-SHA, but the migration-delta needs worktree=v2026.05.2.) Engineer diagnosing the crash + no-delta + reconciling with the working sibling mid-migration-kill. Pivoted operator to run mid-migration-kill (working migrate-kill reference) for breadth + to test shared-vs-migrate-specific. NOTE: operator went quiet on the migrate verdict — I pulled the stage log directly + re-tasked it. Migrate is now a deep multi-layer rabbit hole (version-format → build-timing → missing-checkout → no-delta); if the next fix isn't clean, I'll document it + keep pivoting to other scenarios for morning breadth.

migrate-killed-after-commit: DECISIVELY DIAGNOSED (engineer) → filed STATBUS-013 (King decision A/B/C), DOCUMENTED + DEFERRED. Root cause = STRUCTURAL: the inline ./sb install dispatch runs a crash-recovery BOOT-migrate (install_upgrade.go:198) that consumes the entire v2026.05.2→HEAD delta WITHOUT the inject env (lost across syscall.Exec, service.go:3604) BEFORE the resume-migrate the scenario targets → stall never fires. Service-dispatch scenarios (watchdog-reconnect) have NO boot-migrate → that's why they work. NOT a quick fix (reverses scenario design / touches recovery semantics): A=switch to service-dispatch (engineer's lean), B=fix inline env-propagation (test-only? or prod bug?), C=accept boot-migrate as target + fix env + DB-fragility. NET migrate: 0 product RECOVERY bugs (the recovery code WORKS end-to-end; the scenario's injection model just mismatches the inline two-migrate path); the full fix-chain landed (version-format f018a75d8 → build-timing/Option-A 75ceab1ff → missing-checkout/coherence c9f89f930 → observability b80d725a7) except the final design decision. GRIND MOVES ON to breadth: operator on mid-migration-kill (running; also the env-propagation cross-check), then the other scenarios. RUNNING TALLY: 2 GREEN (0-happy-install, watchdog-reconnect); migrate=diagnosed/deferred(013); archivebackup-resume=recovery-correct, assertion-relax pending architect rec-2; ~12 harness/CI fixes; product findings 010/012/013; 0 confirmed product recovery bugs.
<!-- SECTION:NOTES:END -->
